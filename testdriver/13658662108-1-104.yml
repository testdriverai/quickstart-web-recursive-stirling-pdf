version: 4.2.12
session: 67c72f549c88094147b03bf6
steps:
  - prompt: Focus the search input.
    commands:
      - command: hover-text
        text: Search for features...
        description: search input in the center of the page
        action: click
  - prompt: Type "PDF Multi Tool" into the search bar.
    commands:
      - command: hover-text
        text: Search for features...
        description: search input in the center of the page
        action: click
      - command: type
        text: PDF Multi Tool
  - prompt: Click the search icon or press enter.
    commands:
      - command: press-keys
        keys:
          - enter
  - prompt: Assert that the search results display the "PDF Multi Tool" option.
    commands:
      - command: assert
        expect: the search results display "PDF Multi Tool"
  - prompt: Click on "PDF Multi Tool" under Organize.
    commands:
      - command: hover-text
        text: PDF Multi Tool
        description: PDF Multi Tool under Organize section
        action: click
  - prompt: Assert that the PDF Multi Tool page loads.
    commands:
      - command: assert
        expect: the page displays "PDF Multi Tool"
  - prompt: Check that the tool functionalities are listed.
    commands:
      - command: assert
        expect: the page displays tool functionalities
  - prompt: Assert there are no errors on the screen.
    commands:
      - command: assert
        expect: the page does not display "Error" or "404"
