version: 4.2.12
session: 67c72f5a9c88094147b03c00
steps:
  - prompt: Focus the search input.
    commands:
      - command: hover-text
        text: Search for features...
        description: search input in the center of the page
        action: click
  - prompt: Type "PDF Multi Tool" into the search bar.
    commands:
      - command: hover-text
        text: Search for features...
        description: search input in the center of the page
        action: click
      - command: type
        text: PDF Multi Tool
  - prompt: Click the search icon or press enter.
    commands:
      - command: press-keys
        keys:
          - enter
  - prompt: Assert that the search results display the "PDF Multi Tool" option.
    commands:
      - command: assert
        expect: the search results display "PDF Multi Tool"
  - prompt: Click on the language globe icon.
    commands:
      - command: hover-image
        description: language globe icon in the top right corner
        action: click
  - prompt: Select a different language option.
    commands:
      - command: hover-text
        text: Español
        description: language option in the dropdown
        action: click
  - prompt: Assert that the page changes to the selected language.
    commands:
      - command: assert
        expect: >-
          the page displays text in Spanish such as "Su ventanilla única
          autohospedada para todas sus necesidades PDF"
  - prompt: Assert there are no errors during the language switch.
    commands:
      - command: assert
        expect: the page does not display any error messages such as "Error" or "404"
